package com.example.indroydlab.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * --- Brand Colors ---
 * These define the primary identity of the Lofaz Seller app.
 */
val LofazBlue = Color(0xFF0061AF)       // Primary brand blue 0xFFE2E8F0
val LofazBlueLight = Color(0xFFEFF6FF)  // Light blue variant for containers and backgrounds

/**
 * --- Text Colors ---
 * Used for typography to ensure consistent contrast and readability.
 */
val TextPrimary = Color(0xFF1B2533)     // Main headings and body text (dark navy)
val TextSecondary = Color(0xFF64748B)   // Subtitles, hints, and less prominent info (slate gray)
val TextDark = Color(0xFF0D1B2A)
val TextGray = Color(0xFF6B7A8D)
val TextLight = Color(0xFFB0BAC7)

/**
 * --- Semantic & State Colors ---
 * Used to communicate status, success, errors, or selection states.
 */
val SuccessGreen = Color(0xFF10B981)    // Standard success state / positive actions
val GreenVerified = Color(0xFF00C853)   // Specialized green for verification badges
val ErrorRed = Color(0xFFEF4444)        // Error state / destructive actions
val UnselectedColor = Color(0xFF94A3B8) // Neutral color for unselected icons/tabs 0xFF94A3B8

/**
 * --- Surface & Background Colors ---
 * Define the structure and layering of the application UI.
 */
val PageBg = Color(0xFFF4F7FF)          // Main screen background (light blue tint)
val BackgroundLight = Color(0xFFF8FAFC) // Alternative soft background for contrast
val CardWhite = Color(0xFFFFFFFF)       // Surface color for cards, sheets, and menus
val White = Color(0xFFFFFFFF)           // Pure white for absolute highlights
val BorderColor = Color(0xFFE2E8F0)     // Subtle color for dividers and component borders

// Light Mode Colors (Your Original Colors)
val Blue = Color(0xFF0066CC)
val Black = Color(0xFF000000)
val LightGray = Color(0x1D000000)
val Green = Color(0xFF52CC59)
val Red = Color(0xFFD32F2F)
val DarkBackground = Color(0xFF121212) // Standard Material Dark Background
val DarkSurface = Color(0xFF1E1E1E) // Slightly lighter than background for cards/elevations

//val LightGrayDark = Color(0xFF333333) // Replacing LightGray for dark mode borders/dividers
